#include <iostream>

int main(int argc, char **argv) {
  /* int errcode = 0;
   try{
   	
   } catch(TCLAP::ArgException &e){
   		std::cerr << "Error: " + e.error() + " " + e.argId();
        errcode = 2;
   }
   return errcode;
   */
   return 0;
}
